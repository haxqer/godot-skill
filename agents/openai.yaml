interface:
  display_name: "Godot"
  short_description: "Build, inspect, and debug Godot projects"
  default_prompt: "Use $godot to inspect a Godot project, make scene changes, and debug runs with the bundled Godot workflows."
